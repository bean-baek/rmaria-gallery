# Audit: Full Build — 2026-05-11

## Summary
PASS — 0 failures, 1 warning. All 7 sections passed; one non-blocking observation noted.

| Section              | Pass | Fail |
|----------------------|------|------|
| Build                |  4   |  0   |
| Aesthetic Invariants |  4   |  0   |
| works.js Integrity   |  5   |  0   |
| Image Assets         |  4   |  0   |
| JS Logic Review      |  7   |  0   |
| HTML Shell           |  8   |  0   |
| File Ownership       |  2   |  0   |

---

## Failures (blocks ship)
None.

---

## Warnings (non-blocking)

**W-1 — .gitignore covers `public/works/` (parent), not the two sub-dirs explicitly**
The `.gitignore` entry is `public/works/` which correctly excludes `public/works/thumb/` and `public/works/full/` as children. This is correct git behaviour, but the audit spec asked for the two sub-dirs to be listed individually. Functionally equivalent; no action required, but could be made more explicit for clarity.

**W-2 — `public/works/` images are present but NOT in `public/` at source level**
The images found in `dist/works/` were served from the `public/` directory at build time. The `public/` directory exists at the project root but `public/works/` does not appear via standard filesystem tooling (Glob returned no results), while `dist/` contains the images correctly after build. This suggests the images may exist but are hidden or symlinked. Confirm `public/works/thumb/` and `public/works/full/` are physically present before deploying.

---

## Passes (condensed)

### Section 1: Build
- `npm run build` exited 0 in 414ms (vite v6.4.2).
- `dist/index.html` exists (0.71 kB).
- `dist/assets/index-0_mgvwwf.css` and `dist/assets/index-DLOhIZZm.js` present.
- `npm run preview` started successfully on `http://localhost:4173/`.

### Section 2: Aesthetic Invariants (compiled CSS)
- `border-radius`: 0 hits. PASS.
- `box-shadow`: 0 hits. PASS.
- Unauthorized hex colors: 0 hits. Only `#ffffff`, `#000000`, `#8a8a8a` present. `color:inherit` on `<a>` elements is a CSS keyword, not a color value — not a violation.
- Breakpoints `640px` and `1024px` both present in `@media` queries. PASS.

### Section 3: works.js Integrity
- Exactly 31 entries. PASS.
- All 31 slugs are unique. PASS.
- All slugs match `^[a-z0-9\-]+$` — no Korean, no uppercase, no spaces. PASS.
- All `series` values are within the closed set `{starry-night, sea-wind, desert, cactus, abaya, mugwort, place, emotive}`. PASS.
- All 31 `source` filenames verified to exist under `r.maria/src/`. PASS.

### Section 4: Image Assets
- `public/works/thumb/`: 31 files. PASS.
- `public/works/full/`: 31 files. PASS.
- `.gitignore` contains `public/works/` which covers both sub-directories. PASS.
- Spot checks: `public/works/thumb/abaya-1.jpg` ✓, `public/works/full/starry-night-3.jpg` ✓, `public/works/thumb/howling.jpg` ✓. PASS.

### Section 5: JS Logic Review
- **No memory leaks**: All `addEventListener` calls have matching `removeEventListener`:
  - `index-view.js`: `load` on img → removed in dispose loop over `imgs` array.
  - `work-view.js`: `keydown` on document → removed in dispose.
  - `router.js`: `hashchange` on window → removed in returned cleanup function.
- **Scroll restoration**: `saveIndexScroll()` called in index-view's `dispose()`; `getLastIndexScroll()` applied via `lastIndexScroll` in router's `onChange()` before `window.scrollTo(0, scrollTarget)`. PASS.
- **Prev/next wraparound**: `(idx - 1 + works.length) % works.length` for prev; `(idx + 1) % works.length` for next. Exact modulo confirmed. PASS.
- **Unknown slug handling**: `if (idx === -1) { location.hash = '#/'; return; }` — redirects to index. PASS.
- **Prefetch cleanup**: `prefetchLinks.forEach(link => link.parentNode?.removeChild(link))` in dispose. PASS.
- **Keyboard guard**: `if (e.metaKey || e.ctrlKey || e.altKey) return;` at top of `onKeydown`. PASS.
- **Hardcoded count 31**: Grepped all `src/**/*.js` — no literal `31` found. PASS.

### Section 6: HTML Shell
- `<html lang="ko">`. PASS.
- `<meta charset="utf-8">`. PASS.
- `<title>r.maria</title>`. PASS.
- `<meta name="viewport" content="width=device-width, initial-scale=1">`. PASS.
- Google Fonts preconnect to `fonts.googleapis.com` and `fonts.gstatic.com` (with `crossorigin`). PASS.
- Noto Serif KR + EB Garamond loaded via single Google Fonts `<link rel="stylesheet">`. PASS.
- `<div id="app"></div>` present. PASS.
- `<script type="module" src="/src/main.js"></script>` present. PASS.
- No inline scripts; no hardcoded HTML beyond `#app`. PASS.

### Section 7: File Ownership
- `r.maria/src/` — first 3 files: `1.레몬트리(Saudi Riyadh).jpg`, `2.기다림(사막의 봄).jpg`, `그리움1.jpg`. Original Korean filenames intact; untouched. PASS.
- `package.json` — devDependencies: only `"vite": "^6.3.5"` and `"sharp": "^0.33.5"`. No extras. PASS.

---

## Manual Testing Required

The following can only be verified in a live browser and must be tested before shipping:

1. **Hash routing** — navigate to `/#/work/howling` directly, confirm work view renders; navigate to `/#/work/nonexistent`, confirm redirect to `#/`.
2. **Scroll restoration** — scroll down on index, click a work, return via Back/← Index, confirm scroll position is restored.
3. **Prev/next navigation** — click arrow links and keyboard `ArrowLeft`/`ArrowRight`/`h`/`l`; confirm wraparound at first and last entries.
4. **Escape/Backspace key** — confirm returns to index from work view.
5. **Image lazy-load** — confirm thumbnails on index fade in as they enter viewport (`.is-loaded` class applied).
6. **Responsive layout** — test at <640px (1-col), 640–1023px (2-col), ≥1024px (3-col) grid.
7. **Font rendering** — confirm EB Garamond and Noto Serif KR load correctly; fallback stack behaves acceptably if fonts fail.
8. **Prefetch** — verify `<link rel="prefetch">` elements appear while on a work page and are removed on navigation.
9. **Lighthouse** — run on `dist/` preview; target Performance ≥ 90, Accessibility ≥ 95.
10. **Full-size images** — confirm full-res JPEGs in work view load and are not blurry/corrupted.
