# Audit: v4 Carousel + Lightbox — 2026-05-12

## Summary
PASS — all automated checks green; manual browser verification still required for scroll/touch/visual behaviour.

| Section                       | Pass | Fail |
|-------------------------------|------|------|
| Build                         |  2   |  0   |
| Aesthetic Invariants          |  4   |  0   |
| v4 CSS Classes Present        | 17   |  0   |
| v2 Obsolete Removed           |  5   |  0   |
| index-view.js Logic           | 11   |  0   |
| work-view.js Logic            | 10   |  0   |
| justified-layout.js Untouched |  1   |  0   |

## Failures (blocks ship)
None.

## Warnings (non-blocking)
None.

## Passes (condensed)

### Section 1 — Build
- `npm run build` exited 0 with `vite v6.4.2 ✓ built in 170ms`, 9 modules transformed.
- Bundle sizes (matches stated baseline):
  - `dist/index.html` — 0.71 kB raw / 0.40 kB gzip
  - `dist/assets/index-A0c3uDrz.css` — 7.53 kB raw / 1.88 kB gzip
  - `dist/assets/index-DSZBcoHc.js` — 17.59 kB raw / 5.49 kB gzip

### Section 2 — Aesthetic Invariants (compiled CSS)
- `border-radius` occurrences: 0.
- `box-shadow` occurrences: 0.
- Hex colors found: only `#ffffff` and `#000000` (in `:root` custom-property declarations `--bg`, `--ink`, `--rule`); palette also uses `#8a8a8a` via `--mute`. No `rgb/rgba/hsl/hsla` literals.
- `transform:` appears exactly once in compiled CSS — inside `@media(max-width:639px){...}.lightbox__arrow{position:absolute;top:50%;transform:translateY(-50%);...}`. That selector is NOT inside `:hover` or `:focus-visible`; it is the static-centering pattern explicitly whitelisted by the spec. The `:hover`/`:focus-visible` selectors that do exist (`.masthead__nav-item:hover`, `.series-header__toggle:hover`, `.lightbox__close:hover`, `.lightbox__arrow:hover`, generic `a:hover`) declare only `text-decoration`, `color`, `opacity` — no `transform`.

### Section 3 — v4 CSS Classes Present (compiled)
All present in `dist/assets/index-A0c3uDrz.css`:
- `.series-header__bar`
- `.series-header__toggle`, `.series-header__toggle:hover`, `.series-header__toggle:focus-visible`
- `.series-header__toggle[aria-pressed=true]` (compiled form of `[aria-pressed="true"]`)
- `.series-carousel` (with `--carousel-height` custom property + `min-width:640px` / `min-width:1024px` overrides)
- `.series-carousel__item`
- `.series-carousel__item img.is-loaded`
- `.lightbox`
- `.lightbox__top`
- `.lightbox__close`
- `.lightbox__counter`
- `.lightbox__stage`
- `.lightbox__figure`
- `.lightbox__arrow`
- `.lightbox__caption`
- `.lightbox__title`
- `.lightbox__title-en`
- `.lightbox__meta`
- Mobile `@media(max-width:639px){.lightbox{...}.lightbox__stage{...}.lightbox__arrow{...}.lightbox__arrow--prev{...}.lightbox__arrow--next{...}}`

### Section 4 — v2 Obsolete Classes Removed (compiled CSS)
Combined grep `\.work-header|\.work-nav|\.work-more|\.work__title|\.work__caption` against `dist/assets/index-A0c3uDrz.css`: 0 hits for any of the five.

### Section 5 — `src/views/index-view.js` Logic Review
1. PASS — default mount loop at L182–186 calls `renderCarousel(state.container, group.items)` for every group right after `container.replaceChildren(masthead, main)`. `seriesMode.set(group.key, 'carousel')` at L172 confirms initial mode.
2. PASS — each section header injects `<button class="series-header__toggle mono" data-series="${group.key}" aria-pressed="false">EXPAND</button>` (L158).
3. PASS — `onToggleClick` (L189–207) reads only `e.currentTarget.dataset.series`, reads/writes that one entry in `seriesMode`/`sectionStates`, and replaces only `state.container` (the per-series content `div`). Other sections untouched.
4. PASS — `aria-pressed` toggled at L194: `'true'` when next === 'expanded' else `'false'`.
5. PASS — text toggled at L195: `'COLLAPSE'` when expanded else `'EXPAND'`.
6. PASS — `IntersectionObserver` (L215–230) observes every `.series-section` and adds `is-revealed`; falls back to immediate reveal when API unavailable.
7. PASS — masthead `.masthead__nav-item` click handler (L233–243) reads `data-series`, scrolls target into view, ensures `is-revealed` is set.
8. PASS — `onResize` debounced 200 ms (L246–257) iterates `seriesMode.entries()` and re-renders only entries where `mode === 'expanded'`. Carousel mode is intentionally CSS-only.
9. PASS — `dispose` (L261–272) removes resize listener, disconnects observer, removes nav click listeners, removes toggle click listeners, and removes image `load` listeners across every section's saved `loadHandlers` (covers both modes because handlers are re-saved on each mode swap and on resize re-layouts; old handlers are removed at L199 / L251 before re-render).
10. PASS — `seriesMode.clear()` and `sectionStates.clear()` at L270–271.
11. PASS — counts driven entirely by `works.length`, `groups.length`, and `group.items.length`; no hardcoded numbers.

### Section 6 — `src/views/work-view.js` Logic Review
1. PASS — `lightbox.setAttribute('role', 'dialog')` + `aria-modal="true"` + `aria-label` (L18–22).
2. PASS — `const sameSeries = works.filter(w => w.series === work.series)` (L11), prev/next computed from `sameSeries` only.
3. PASS — wraparound: `(idxInSeries - 1 + sameSeries.length) % sameSeries.length` and `(idxInSeries + 1) % sameSeries.length` (L13–14).
4. PASS — counter built as `${NN} / ${SS} · ${seriesEnTitle}` where `seriesEnTitle = seriesMeta[work.series].titleEn.toUpperCase()` (L15, L24).
5. PASS — `prevBodyOverflow = document.body.style.overflow; document.body.style.overflow = 'hidden'` at render (L52–53); restored to `prevBodyOverflow` in `dispose` (L95).
6. PASS — `onBackdropClick` guards `if (e.target === lightbox)` (L66–70); clicks on inner figure/caption/arrows do not match the container itself.
7. PASS — `onKeydown` (L74–87) early-returns when `metaKey || ctrlKey || altKey` set, also skips when focus is in `INPUT`/`TEXTAREA`. Handles `ArrowLeft`/`h`, `ArrowRight`/`l`, `Escape`/`Backspace` (preventDefault on close to suppress browser back).
8. PASS — `<link rel="prefetch" as="image">` injected for prev and next at L56–63; collected in `prefetchLinks`; removed in dispose at L94 via `link.parentNode?.removeChild(link)`.
9. PASS — every listener paired: backdrop click added L71 / removed L92; keydown added L88 / removed L93.
10. PASS — unknown slug short-circuits with `location.hash = '#/'; return;` at L5–8.

### Section 7 — `src/views/justified-layout.js` Untouched
Presence verified — exports `JUSTIFIED_GAP`, `getTargetHeight`, `getMaxHeight`, `buildRows`, `renderJustifiedRows`, `debounce`. All required v3 exports (`renderJustifiedRows`, `getTargetHeight`, `buildRows`, `debounce`) are present at expected line numbers. (Repo is not under git here, so byte-level diff against v3 not possible; presence check satisfies the brief.)

## Requires Browser Verification
- Each series renders as horizontal carousel by default.
- Horizontal scroll works via mouse wheel (with shift) / trackpad / touch.
- Scroll snap snaps to image boundaries (`scroll-snap-type: x mandatory` + `scroll-snap-align: start` confirmed in CSS).
- EXPAND button on a series swaps that series only to justified rows.
- COLLAPSE returns to carousel.
- Other series remain in their current mode while one is toggled.
- Visiting index after navigating away resets all to carousel (state is created inside `render()` so in-memory state is fresh on each mount — confirm visually).
- Click any image: lightbox opens full-screen on black background.
- URL changes to `#/work/{slug}`.
- ← / → arrow keys move prev/next within the same series, wrapping at ends.
- Esc / ✕ / clicking the black backdrop (NOT on image or caption) closes.
- Closing returns to `#/` and restores scroll position (`saveIndexScroll()` called in index dispose; router responsible for restore).
- body scroll is locked when lightbox is open (page behind doesn't scroll).
- Mobile 375 px: lightbox arrows positioned absolutely on sides, image fits viewport.
- Mobile: carousel still works via touch swipe.
