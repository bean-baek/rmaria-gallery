{
  "name": "rmaria-gallery",
  "version": "1.0.0",
  "type": "module",
  "private": true,
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "build:full": "npm run images && npm run build",
    "preview": "vite preview",
    "images": "node scripts/optimize-images.mjs"
  },
  "devDependencies": {
    "sharp": "^0.33.5",
    "vite": "^6.3.5"
  }
}
